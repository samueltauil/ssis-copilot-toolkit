# SQL scripts live here.
# Conventions:
#   - Numeric prefix denotes run order (01-*, 02-*, ...).
#   - validate-<PackageName>.sql files are emitted by /generate-validation-sql.
# Run via Invoke-Sqlcmd; never via SSIS (SSIS is for ETL, not DDL).
